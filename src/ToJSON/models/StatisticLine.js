/**
 * Class with informations about a line
 */
export default class StatisticLine {
  constructor(lineNumber, line, text) {
    this.LineNumber = lineNumber;
    this.Line = line;
    this.Text = text;
  }
}
